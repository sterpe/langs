// You are tasked with defining and implementing a function.
// As input you are given an N x M matrix.  An `x` may appear
// any number of times in a matrix.  Your function should 
// modify the `b` matrix such that any row and column where 
// `x` originally appears are completely over-written with 
// `x`'s

module.exports = function ƒ(matrixA) {
	var matrixB = [];

	return matrixB;
};
